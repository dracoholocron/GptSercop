# HorizontalPodAutoscaler opcional para la API.
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: sercop-api
  labels:
    app: sercop-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: sercop-api
  minReplicas: 1
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 80
