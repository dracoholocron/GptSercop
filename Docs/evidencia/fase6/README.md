# Fase 6 – Subasta Inversa Electrónica (SIE) – MVP

Evidencia del flujo SIE en el portal proveedor.

| Archivo | Descripción |
|---------|-------------|
| supplier-procesos.png | Lista de procesos con enlaces "Presentar oferta" y "SIE (MVP)". |
| supplier-sie.png | Pantalla SIE (si hay procesos): estado, mejor oferta, formulario de envío. Se genera al ejecutar evidencia con al menos un proceso publicado. |

Generación: `npm run evidence:full` (o `npx playwright test e2e/evidence.spec.ts`).
